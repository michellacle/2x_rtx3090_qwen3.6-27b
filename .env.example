# Qwen3.6-27B Server Configuration
# Copy to .env and edit, or export these before running serve.sh

# Model path (must exist on disk)
# MODEL_PATH=/home/michel/models/qwen3.6-27b-fp8

# Server
VLLM_PORT=8000
VLLM_TP=2
VLLM_GPU_MEM=0.88
VLLM_MAX_LEN=131072
VLLM_MAX_SEQS=2

# Pre-flight only (don't start server, just check GPU/venv)
# VLLM_CHECK_ONLY=1
